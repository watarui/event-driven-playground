# ===================================
# CI/CD ビルド専用の環境変数
# ===================================
# CI でのビルド時に使用するダミー値
# 実際の認証は不要なため、形式的に有効な値を設定

# GraphQL エンドポイント（CI では使用しないが形式的に設定）
NEXT_PUBLIC_GRAPHQL_ENDPOINT=http://localhost:4000/graphql
NEXT_PUBLIC_WS_ENDPOINT=ws://localhost:4000/socket/websocket

# Firebase Configuration（ダミー値）
# ビルド時の型チェックを通すための最小限の設定
NEXT_PUBLIC_FIREBASE_API_KEY=dummy-api-key-for-ci-build
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=dummy-project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=dummy-project
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=dummy-project.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=123456789
NEXT_PUBLIC_FIREBASE_APP_ID=1:123456789:web:dummyappid

# 注意: CI では Firebase Admin SDK は使用しないため、
# FIREBASE_PROJECT_ID, FIREBASE_CLIENT_EMAIL, FIREBASE_PRIVATE_KEY は不要